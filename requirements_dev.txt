# Python development dependencies for the messagehub integration.
# Production-Dependencies werden in custom_components/messagehub/manifest.json gepflegt.

# Test stack
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
pytest-homeassistant-custom-component>=0.13
# Hinweis: pytest-homeassistant-custom-component zieht homeassistant als Dep.

# Runtime-Dependencies (parallel zu manifest.json)
# Werden hier wiederholt, damit Tests ohne HA-Container laufen können.
aiosqlite>=0.20
jsonpath-ng>=1.6

# Code quality
ruff>=0.6
mypy>=1.10
pre-commit>=3.7

# Optional: für Smoke-Tests gegen laufende Dev-HA
httpx>=0.27
