{
  "title": "Message Hub",
  "config": {
    "step": {
      "user": {
        "title": "Set up Message Hub",
        "description": "Confirm the setup. Webhooks and further options are managed inside the panel."
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance of this integration is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "description": "Retention, hard cap and log level of the integration.",
        "data": {
          "retention_debug_days": "Retention debug (days)",
          "retention_info_days": "Retention info (days)",
          "retention_warning_days": "Retention warning (days)",
          "retention_error_days": "Retention error (days)",
          "hard_cap_total": "Maximum number of messages",
          "aggregation_window_minutes": "Aggregation window (minutes)",
          "log_level": "Log level"
        }
      }
    }
  },
  "services": {
    "add_message": {
      "name": "Add message",
      "description": "Manually adds a message to the message hub.",
      "fields": {
        "severity": { "name": "Severity", "description": "Severity level" },
        "source": { "name": "Source", "description": "Logical origin" },
        "text": { "name": "Text", "description": "Message text" },
        "metadata": { "name": "Metadata", "description": "Optional extra fields" }
      }
    }
  },
  "issues": {
    "knx_unavailable": {
      "title": "KNX integration unavailable",
      "description": "Message Hub cannot read the KNX bus directly because the HA-KNX integration is not running (or has an old module layout). We fell back to the `knx_event` bus; for telegrams to arrive, you must add a `knx: event:` list with the relevant group addresses to `configuration.yaml`. Recommended: restart or update HA-KNX."
    },
    "mqtt_unavailable": {
      "title": "MQTT integration unavailable",
      "description": "Message Hub has MQTT topic subscriptions configured, but the HA-MQTT integration is not running. Set up the MQTT integration so incoming topics show up as messages."
    }
  }
}
